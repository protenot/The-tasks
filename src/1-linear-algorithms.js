export function multiply(a, b) {
  console.log(a + b);
  console.log(a * b);
  return a + b + " и " + a * b;
}
export function summarize(string, superstring) {
  console.log(string.length + superstring.length);
  return string.length + superstring.length;
}
